# pip install -r requirements.txt --break-system-packages   (if on an externally-managed env)
piper-tts>=1.8.0

# system dependency, not pip-installable: ffmpeg (only needed if you output .mp3 instead of .wav)
